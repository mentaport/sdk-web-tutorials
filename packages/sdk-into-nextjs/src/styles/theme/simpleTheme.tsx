import { createTheme } from '@mui/material/styles';

const simpleTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#FB448B',
    },
    secondary: {
      main: '#f3fb76',
    },
  },
});

export default simpleTheme;
